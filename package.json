{
  "name": "app",
  "scripts": {
    "build:css": "sass ./app/assets/stylesheets/application.bootstrap.scss --no-source-map --load-path=node_modules"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.2",
    "bootstrap-icons": "^1.9.1",
    "sass": "^1.55.0"
  }
}
